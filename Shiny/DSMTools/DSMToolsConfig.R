rootDir <- 'Data'
currentUser <- 'Demo'  ## will propbably replace this with some login functionality down the track
UseReducedResRasters <- T  # Displays reduce resolution rasters - sppeds u releaflet rendering
rasterResFactor <- 5
minBlocks <- 30   ##  controls how much the parallel processing is chunked up. The bigger the number the lesser the memory requirement


helpColour = 'green'
